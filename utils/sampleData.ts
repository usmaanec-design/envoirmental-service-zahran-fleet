// Sample data creation utility - DISABLED for production
// Only real Zahran/Sharq projects allowed

export const createSampleProjects = async () => {
  console.log(' Sample project creation is disabled for production use.');
  console.log(' No test projects will be created. Only real Zahran/Sharq projects allowed.');
  return;
};

export const deleteSampleData = async () => {
  console.log(' Sample data creation is disabled - no cleanup needed.');
  return;
};